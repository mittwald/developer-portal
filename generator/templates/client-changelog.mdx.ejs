---
<%- yaml.stringify({
    title: `${name} ${release.name} released`,
    tags: [
      repo
    ]
}) %>
---

Version <%= release.name %> of the <%= name %> package has been released.

{/* truncate */}

<%- release.body %>